# --- VIRTUAL ENVIRONMENT ---
# Ignore the virtual environment folder. This folder contains all the downloaded
# libraries and can be very large. Anyone can recreate it using the
# requirements.txt file, so it should never be tracked.
/venv
/env

# --- PYTHON CACHE & ARTIFACTS ---
# Ignore the Python cache folder, compiled files, and other temporary files
# that Python creates automatically to run your code faster.
__pycache__/
*.pyc
*.pyo
*.pyd

# --- SECRETS ---
# Ignore the .env file. This is CRITICAL for security. This file contains
# your database password and secret keys and must never be shared on GitHub.
.env

# --- IDE & EDITOR CONFIGURATION ---
# Ignore configuration files created by code editors like VS Code or PyCharm.
# These are specific to your local machine and shouldn't be shared.
.vscode/
.idea/

# --- OPERATING SYSTEM FILES ---
# Ignore system-specific files created by macOS, Windows, or Linux.
.DS_Store
Thumbs.db

# --- LOGS ---
# Ignore log files. Log files can grow very large and contain sensitive info.
*.log

# --- TESTING ---
# Ignore files and folders generated by testing tools like pytest.
.pytest_cache/
.coverage